# External Prometheus Configuration
# Copy this file to .env and update with your production values

# URL for your shared external Prometheus remote write endpoint
# Example: https://prometheus.example.com/api/v1/write
PROMETHEUS_REMOTE_WRITE_URL=https://your-prometheus-service.com/api/v1/write

# Authentication token for Prometheus remote write (if required)
# Leave empty if no authentication is needed
PROMETHEUS_AUTH_TOKEN=your-bearer-token-here

# Alternative: Basic Auth (if using basic authentication instead of bearer token)
# PROMETHEUS_USERNAME=your-username
# PROMETHEUS_PASSWORD=your-password